# 第三阶段记录：通信故障注入与鲁棒性量化（Phase 3）

目标：在编队控制链路中注入**邻机状态通信故障**（延迟、丢包、噪声），量化编队性能退化，并验证一种延迟补偿方法。
实验边界（对应任务书要求）：**故障只存在于机间通信链路**，每架无人机自身传感器/控制始终完好。

## 新增内容

| 文件 | 说明 |
| --- | --- |
| `gym_pybullet_drones/utils/comm_link.py` | 通信链路模型 `CommLink`：延迟（步）、丢包（概率）、噪声（高斯 std）、确定性种子 |
| `formation_flight.py` | 新增 `--mode leader-follower` 领随架构与故障参数 `--delay_steps/--loss/--noise/--compensate/--seed`（重构） |
| `sweep_faults.py` | 批量故障扫描：输出 `results/fault_sweep.csv` 与退化曲线 `results/fault_sweep.png` |

## 实验设计

- 控制架构改为**领机-跟随机**：领机（drone0）沿参考圆飞行；跟随机（drone1）的目标点 = **经通信链路收到的领机位置** + 编队偏置（1m）。领机状态（位置+速度）每控制步（48Hz，20.8ms）发送一次。
- 故障注入点：仅 `CommLink`。跟随机自身状态无任何退化；丢包时接收端保持最近一次成功收到的样本（零阶保持）。
- 补偿方法（对应第四阶段改进点的提前实现）：**速度外推**（dead reckoning）——用收到的领机速度对样本龄期外推位置：`p_est = p_rx + v_rx · age`。
- 指标：**矢量队形误差** `‖p1 - p0 - 偏置‖`（方向感知，主指标）、机间距误差、领机轨迹 RMSE（稳态 t>2s）。
- 每回合 12s；链路种子固定为 0。

## 扫描结果（矢量队形误差均值，mm；完整数据见 `results/fault_sweep.csv`）

### 延迟扫描（含/不含补偿）

| 延迟 | 步数 | 原始 | 补偿后 |
| --- | --- | --- | --- |
| 0 ms | 0 | 10.9 | 10.9 |
| 21 ms | 1 | 12.5 | 11.0 |
| 42 ms | 2 | 12.8 | 13.1 |
| 83 ms | 4 | 19.0 | 16.7 |
| 167 ms | 8 | 35.1 | **20.0** |
| 250 ms | 12 | 48.3 | **28.4** |
| 333 ms | 16 | 65.5 | **35.7** |

结论：延迟超过 ~83ms 后退化近似线性增长，333ms 时误差达无故障水平的 **6 倍**；速度外推补偿对 ≥167ms 的延迟稳定恢复约 **40-50%** 的退化，小延迟（≤42ms）时无收益甚至轻微为害（外推引入噪声）。

### 丢包扫描（无延迟）

| 丢包率 | 0% | 10% | 20% | 30% | 50% |
| --- | --- | --- | --- | --- | --- |
| 队形误差 (mm) | 10.9 | 11.8 | 9.8 | 12.2 | 13.2 |

结论：**高鲁棒**。48Hz 下即使 50% 丢包，"保持最近样本"策略等效于平均仅滞后约 1 步（21ms）的延迟，退化不足 2mm。丢包在这个时间尺度上近似等价于小延迟。

### 噪声扫描（无延迟）

| 噪声 std | 0 | 1cm | 2cm | 5cm | 10cm |
| --- | --- | --- | --- | --- | --- |
| 队形误差 (mm) | 10.9 | 11.9 | 13.8 | 23.6 | 42.3 |

结论：接收噪声近似单调传递到队形误差（PID 位置环起低通滤波作用，衰减约 1/4-1/2）。

## 开发过程中发现并修复的问题

1. 领随模式初版跟随机目标误用了**绝对**编队偏置（+0.5m）而非**相对**偏置（+1.0m），造成恒定 0.5m 队形误差，掩盖全部故障效应——由故障试跑的"各故障配置误差几乎相同"这一异常信号定位。
2. 纯位置 PID 跟踪移动目标存在固有的速度比例滞后（v/kp），已通过向跟随机的 `DSLPIDControl.computeControl` 传递 `target_vel`（领机速度）抑制。
3. 丢包瞬间目标为空导致控制崩溃——改为零阶保持最近有效目标。

## 与后续阶段的衔接

- 组合故障（延迟+丢包+噪声同时注入）、RL 链路（FormationAviary 观测经链路获取）的同等实验为自然扩展。
- 若需要更强补偿（如二阶外推/ Smith 预估器/ 基于估计器的控制），可在 `CommLink.recv()` 的龄期信息上直接构建，与本阶段接口兼容。
